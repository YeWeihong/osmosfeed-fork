{
  "name": "@osmoscraft/osmosfeed",
  "version": "0.0.1",
  "description": "Monorepo root",
  "directories": {
    "doc": "docs"
  },
  "bin": {
    "osmosfeed": "./packages/cli/main.js"
  },
  "scripts": {
    "prepare": "npm run build",
    "start": "npm run dev -w packages/cli -w packages/sandbox",
    "build": "npm run build -w packages/cli",
    "test": "npm run test -w packages/test -w packages/cli",
    "clean": "npm run clean --workspaces=true"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/YeWeihong/osmosfeed-fork.git"
  },
  "author": "osmoscraft",
  "license": "MIT",
  "workspaces": [
    "./packages/*"
  ]
}
